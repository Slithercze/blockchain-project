package com.gfa.blockchaingfa.models;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class BlockDataTest {
    /*

    @Test
    void setHash() {
        // arrange:
        BlockData blockData = BlockData.builder()
                .data("test No. 1")
                .timestamp(1689151313)
                .build();
        String expectedHash = "c11b8f4bb894bf16b8efe51b40f7fb92a6c384d61c04b365260bb9c444118f7e";
        // act:
        blockData.setHash();
        String receivedHash = blockData.getHash();
        // assertion:
        Assertions.assertThat(receivedHash).isEqualTo(expectedHash);
    }

     */
}